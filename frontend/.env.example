# Fastify recommendation backend (repo `backend/`). Read server-side only.
BACKEND_URL=http://127.0.0.1:3001

# Optional MapLibre style URL. Empty uses OpenFreeMap Positron (OpenStreetMap data).
NEXT_PUBLIC_MAP_STYLE_URL=
