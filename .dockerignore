# 必須排除 node_modules：本機是 Windows 建的，裡面的原生模組
# （better-sqlite3）在 linux 映像檔裡不能用，而且會蓋掉 pnpm install 的結果。
node_modules
**/node_modules

.next
out
dist

# 種子資料庫由映像檔建置時重新產生（見 Dockerfile）
data
!data/.gitkeep

# 後端有自己的 Dockerfile 與 build context
backend

.git
.github
deploy
e2e
docs
playwright-report
test-results
coverage

.env
.env.local
.env.prod
*.log
.DS_Store
AGENTS.md
CLAUDE.md
